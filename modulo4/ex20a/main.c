#include <stdio.h>
#include "c.h"
 
int main(void){
	char x = 10;
	char vec[]={1,2,3,4,0};

	sum_multiples_x(vec,x);
	
	while(vec[i] != 0)
	{
		printf("vec2[%d]=%d\n"i,vec[i]);
		i++;
	}
	
	return 0;
}
